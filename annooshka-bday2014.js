// Generated by CoffeeScript 1.6.3
(function() {
  var Background, Ball, Block, Game, Platform,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Background = (function() {
    function Background(color) {
      this.color = color;
    }

    Background.prototype.redraw = function(ctx) {
      ctx.fillStyle = this.color;
      return ctx.fillRect(0, 0, ctx.canvas.width, ctx.canvas.height);
    };

    return Background;

  })();

  Platform = (function() {
    function Platform(x, y, width, height, delta) {
      this.x = x;
      this.y = y;
      this.width = width;
      this.height = height;
      this.delta = delta;
    }

    Platform.prototype.step_left = function() {
      return this.x = Math.max(0, this.x - this.delta);
    };

    Platform.prototype.step_right = function() {
      return this.x = Math.min(ctx.canvas.width - this.width, this.delta);
    };

    Platform.prototype.redraw = function(ctx) {
      ctx.fillStyle = "#fff";
      return ctx.fillRect(this.x, this.y, this.width, this.height);
    };

    return Platform;

  })();

  Block = (function() {
    function Block(x, y, width, height, type) {
      this.x = x;
      this.y = y;
      this.width = width;
      this.height = height;
      this.type = type;
    }

    Block.prototype.shot = function() {
      return this.type = Math.max(-1, this.type - 1);
    };

    Block.prototype.redraw = function(ctx) {
      switch (this.type) {
        case 1:
          ctx.fillStyle = "#f00";
          break;
        case 0:
          ctx.fillStyle = "#0f0";
          break;
        case -1:
          return;
      }
      return ctx.fillRect(this.x, this.y, this.width, this.height);
    };

    Block.build_blocks_from_map = function(map, sx, sy, width, height) {
      var blocks, blocks_line, ch, ix, iy, line, n, _i, _j, _len, _len1, _ref, _ref1;
      blocks = [];
      iy = 0;
      _ref = map.split("\n");
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        line = _ref[_i];
        ix = 0;
        blocks_line = [];
        _ref1 = line.trim();
        for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
          ch = _ref1[_j];
          switch (ch) {
            case ".":
              n = 0;
              break;
            case "#":
              n = 1;
          }
          blocks.push(new Block(sx + ix * width, sy + iy * height, width, height, n));
          ix += 1;
        }
        iy += 1;
      }
      return blocks;
    };

    return Block;

  })();

  Ball = (function() {
    function Ball(x, y, dx, dy, radius) {
      this.x = x;
      this.y = y;
      this.dx = dx;
      this.dy = dy;
      this.radius = radius;
    }

    Ball.prototype.redraw = function(ctx) {
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.radius, 0, 2 * Math.PI, false);
      ctx.fillStyle = "#00f";
      return ctx.fill();
    };

    return Ball;

  })();

  Game = (function() {
    function Game(ctx, static_objects, platform, ball, w, h, interval) {
      this.ctx = ctx;
      this.static_objects = static_objects;
      this.platform = platform;
      this.ball = ball;
      this.w = w;
      this.h = h;
      this.interval = interval != null ? interval : 30;
      this.iteration = __bind(this.iteration, this);
      setInterval(this.iteration, this.interval);
    }

    Game.prototype.iteration = function() {
      var _this = this;
      this.static_objects.forEach(function(e) {
        return e.redraw(_this.ctx);
      });
      this.ball.redraw(this.ctx);
      this.platform.redraw(this.ctx);
      return this.ball.redraw(this.ctx);
    };

    return Game;

  })();

  $(function() {
    var blk_width, blocks_map, ctx, g, h, static_objects, w;
    $("body").css("background", "#293134");
    ctx = $("#area")[0].getContext("2d");
    w = $(window).width() - 20;
    h = $(window).height() - 20;
    ctx.canvas.width = w;
    ctx.canvas.height = h;
    blocks_map = '.................\n......##.##......\n.....#..#..#.....\n......#...#......\n.......#.#.......\n........#........\n.................';
    static_objects = [new Background("#293134")];
    blk_width = w / blocks_map.split("\n")[0].length;
    static_objects = static_objects.concat(Block.build_blocks_from_map(blocks_map, 0, 0, blk_width, h / 12));
    return g = new Game(ctx, static_objects, new Platform(w * 0.9 / 2, h * 0.975 - 5, w * 0.1, h * 0.025, 5), new Ball(w / 2, h * 0.8, 1, -1, 10), w, h);
  });

}).call(this);
